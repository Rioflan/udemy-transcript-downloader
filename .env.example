UDEMY_EMAIL=your-email@example.com

# Optional: Use cookies for authentication (skips email + verification code)
# Export from browser using a cookie export extension, then paste JSON here or use cookies.json file
# UDEMY_COOKIES=[{"name":"access_token","value":"...","domain":".udemy.com"}]

# Optional: Enable debug mode (saves screenshots on errors, verbose logging)
# DEBUG=true